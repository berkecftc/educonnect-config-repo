server:
  port: 9002

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=auth_db
    username: eduadmin
    password: edu123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema: auth_db

  rabbitmq:
    host: localhost # Docker'daki RabbitMQ servisi
    port: 5672
    username: guest
    password: guest

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

jwt.secret: "Base64:jZwU5GubnUxm1GesQMzbti2lhVckdkLpww1Awn3h0ng="
jwt.expiration-ms: 86400000 
    
