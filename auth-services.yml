server:
  port: 9001
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/auth_db # DİKKAT: 'postgres' yerine 'localhost'
    username: eduadmin
    password: edu123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
