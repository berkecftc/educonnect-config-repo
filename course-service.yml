server:
  port: 8084

spring:
  application:
    name: course-service
  
  datasource:
    # Veritabanı adını 'postgres' yapmışsın, eğer 'educonnect' kullanıyorsan onu düzelt.
    # Şema ismin doğru: course_db
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=course_db
    username: eduadmin
    password: edu123
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    open-in-view: false

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true

# 👇 MİNIO AYARLARI (DÜZELTİLDİ)
minio:
  url: http://localhost:9000
  access-key: your_minio_user  # Gerçek kullanıcı adını yaz (genelde minioadmin)
  secret-key: your_minio_password  # Gerçek şifreni yaz (genelde minioadmin)
  bucket-name: courses-bucket # ⚠️ DİKKAT: 'bucket:' altına girinti yapma, tire (-) ile birleştir.

jwt:
  secret: "Base64:jZwU5GubnUxm1GesQMzbti2lhVckdkLpww1Awn3h0ng="

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000