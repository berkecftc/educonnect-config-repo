server:
  port: 9003
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/course_db # DİKKAT: 'localhost'
    username: eduadmin
    password: edu123
  rabbitmq:
    host: localhost # DİKKAT: 'rabbitmq' yerine 'localhost'
    port: 5672
    username: guest
    password: guest
    minio:
    url: http://localhost:9000
    access-key: your_minio_user     # docker-compose'da belirlediğiniz kullanıcı adı
    secret-key: your_minio_password # docker-compose'da belirlediğiniz şifre
    bucket:
    name: educonnect # Kullanacağınız varsayılan bucket adı
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/